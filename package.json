{
  "name": "haiti-sportsbook-mvp",
  "version": "0.1.0",
  "private": true,
  "description": "Offline-first, data-optimized mobile sportsbook MVP for Haiti",
  "scripts": {
    "start": "node server.js",
    "start:balance": "node mock-balance-api.js",
    "start:backend": "node backend/server.js",
    "build:web": "node scripts/build-web.js",
    "cap:sync": "npm run build:web && npx cap sync android",
    "android:open": "npx cap open android",
    "android:apk:debug": "npm run cap:sync && cd android && .\\gradlew.bat assembleDebug"
  },
  "devDependencies": {
    "@capacitor/android": "^8.3.0",
    "@capacitor/cli": "^8.3.0",
    "@capacitor/core": "^8.3.0"
  }
}
